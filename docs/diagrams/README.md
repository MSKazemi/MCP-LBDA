# Diagrams

Add visual aids here to support the course material.

## Recommended Diagrams

| File | Purpose |
|------|---------|
| `mcp-overview.png` | High-level: LLM, Client, Server, Tools, Resources |
| `client-server-flow.png` | Request/response flow for a tool call |
| `tool-execution-flow.png` | Step-by-step tool call lifecycle |

## Format

- PNG or SVG
- Simple, readable at small size
- Use `docs/03-architecture.md` for ASCII fallback

## ASCII Reference (from docs)

```
User → Model → Tool → Server → Result → Model → User
```
